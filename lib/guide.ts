// The student's guide has a single, fixed name across the whole product.
// It is never derived from users.alien_name (a deprecated column that held
// drifted per-student names) and never falls back to "Scout".
export const GUIDE_NAME = 'Orin';
